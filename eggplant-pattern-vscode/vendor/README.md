This directory contains packaged runtime assets needed by the published VSIX.

- `viz.cjs` is copied from `@viz-js/viz` version `3.25.0`.
- The upstream package is MIT licensed.
- Keep `package.json` in sync with the upstream dependency version when refreshing this vendored file.
